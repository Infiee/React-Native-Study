```
yarn add zustand axios ahooks dayjs immer react-native-logs @tencentcloud/chat tim-upload-plugin @react-native-community/netinfo @tanstack/react-query @react-navigation/elements
```

```
npx expo install expo-network
npx expo install react-native-mmkv react-native-nitro-modules
npx expo install @shopify/flash-list
npx expo install expo-device
```

tailwind 暂时支持web，不支持android和ios
https://docs.expo.dev/guides/tailwind/
